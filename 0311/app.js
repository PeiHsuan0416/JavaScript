// let list = ["a", "b", "c"];

// list.push("d");
// list.unshift("e");
// list.shift;
// list.pop;

// console.log(list);

const list = ["a", "b", "c", "d", "e"];
for (let i = 0; i < list.length; i++) {
  if (i == 2) break;
  console.log(list[i]);
}
